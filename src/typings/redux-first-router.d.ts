import { Action } from 'redux-first-router';

declare module 'redux-first-router' {
  // TODO: make this work since the provided types are not correct
  // export function redirect(where: { type: string }): Action;
}
